# Preflight Static Demo

This fallback demo is the verified local build when dependency installation is unavailable.

Open:

```powershell
static-demo\index.html
```

The demo runs entirely in the browser with deterministic data. It does not require `OPENAI_API_KEY`, Tavily, Vercel, a database, or network access.
